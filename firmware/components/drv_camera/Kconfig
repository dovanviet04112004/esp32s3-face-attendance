menu "Camera driver"

    config DRV_CAMERA_SELFTEST
        bool "Offer drv_camera_selftest()"
        default y
        help
            Grabs twenty frames and logs their geometry and rate.
            Bring-up only: it blocks the boot for about a second.

endmenu
