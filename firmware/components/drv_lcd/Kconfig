menu "LCD driver"

    config DRV_LCD_SELFTEST
        bool "Offer drv_lcd_selftest()"
        default y
        help
            Walks the panel through four colours, holding each for 700 ms.
            Bring-up only: it costs about three seconds of every boot.

endmenu
