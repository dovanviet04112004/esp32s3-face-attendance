# Postgres 16 defaults that matter, plus what this deployment adds.
listen_addresses = '*'
max_connections = 100
shared_buffers = 256MB

hba_file = '/etc/postgresql/pg_hba.conf'

include = '/etc/postgresql/archive.conf'
